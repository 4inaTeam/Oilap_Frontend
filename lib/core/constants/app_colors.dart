import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const Color mainColor = Color(0xFF3A5B22);
  static const Color textColor = Color(0xFF1B2A16);

  // Statut de facture ou produit
  static const Color encoursColor = Color(0xFF002AFF);
  static const Color enattantColor = Color(0xFFFF0000);
  static const Color finiColor = Colors.lightGreen;

  // Arrière-plans
  static const Color backgroundLight = Color(0xFF83927F);
  static const Color backgroundDark = Color(0xFF303030);

  // Couleurs supplémentaires
  static const Color success = Color(0xFF3A5B22);
  static const Color error = Color(0xFFFB3B3B);

  // Couleurs de button
  static const Color buttonColor = Color(0xFF0F3403);

  static const Color accentYellow = Color(0xFFFEDE00);
  static const Color accentGreen = Color(0xFF3A5B22);
}
